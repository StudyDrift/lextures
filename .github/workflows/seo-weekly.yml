name: Weekly SEO governance

on:
  schedule:
    - cron: '23 7 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-and-lifecycle:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: www
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: www/package-lock.json
      - run: npm ci
      - name: Exhaustive live verification
        run: npm run seo:smoke -- --origin=https://lextures.com --exhaustive
      - name: Generate lifecycle report
        run: npm run seo:lifecycle
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: seo-lifecycle-${{ github.run_id }}
          path: www/dist/seo-lifecycle.md
          retention-days: 90
