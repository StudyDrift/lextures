name: Deploy marketing site (www)

on:
  push:
    branches: [main]
    paths:
      - 'www/**'
      - '.github/workflows/pages-www.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-www
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: www
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: www/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint (oxlint)
        run: npm run lint

      - name: Build
        env:
          # Public marketplace API may not be deployed yet; prerender uses SKIP until then.
          SKIP_COURSE_PRERENDER: "1"
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: www/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
