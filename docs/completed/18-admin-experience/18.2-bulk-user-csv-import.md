# 18.2 — Bulk User CSV Import / Merge

> Implementation plan. Source: [docs/MISSING_FEATURES.md](../../MISSING_FEATURES.md) §18.

## Metadata

| Field | Value |
|---|---|
| **Feature ID** | 18.2 |
| **Section** | Admin Experience |
| **Severity** | BLOCKER |
| **Markets** | K12, HE |
| **Status (today)** | IMPLEMENTED |
| **Estimated effort** | M (2–4 w) |
| **Owner (proposed)** | Platform / Backend team |
| **Depends on** | 18.1 (Admin console shell), 17.3 (Background job queue) |
| **Unblocks** | 4.3 (OneRoster CSV baseline before full API), onboarding for pilot cohorts |

---

## Implementation notes

Shipped on branch `fix/bulk-user-csv-import-4025`:

- **API:** `POST/GET /api/v1/admin-console/imports` with multipart upload, dry-run, merge strategies (`create_only`, `upsert`, `sync`), and job status polling.
- **Profiles:** `lextures_native` and `oneroster_v1.2` (maps `sourcedId → external_id`).
- **Storage:** `provisioning.user_import_jobs` table; `users.external_id` upsert key; background `users.import` job type with inline fallback when `BACKGROUND_JOBS_ENABLED` is off.
- **UI:** `/org-admin/import` (`AdminImport.tsx`) gated by `bulkCsvImportEnabled` platform flag (requires `adminConsoleEnabled`).
- **Tests:** `server/internal/service/csvimport/parse_test.go`, `admin_import_nodb_test.go`, `e2e/tests/bulk-user-import.spec.ts`.

---

## 1. Problem Statement

There is currently no way for a district admin or institution IT staff to provision users in bulk. Every user must register individually or be created one at a time through ad-hoc tooling. K-12 districts and universities routinely onboard hundreds to tens of thousands of students and staff at the start of each academic year and cannot afford manual, per-user flows. This gap is a BLOCKER for any institutional sale and is closely related to the OneRoster CSV standard (§4.3), which Lextures must eventually fully implement.

## 2. Goals

- Allow an `org_admin` to upload a CSV file that creates, updates, or deactivates users in bulk within their organization.
- Support both a simple Lextures-native CSV format and the OneRoster 1.2 `users.csv` column schema as a recognized import profile.
- Run the import asynchronously via the background job queue with progress reporting.
- Provide a dry-run / preview mode showing row-level parse errors before any DB writes.
- Emit an admin audit-log event for every user created, updated, or deactivated by the import.

*(Remaining sections unchanged from original plan — see git history for full specification.)*
