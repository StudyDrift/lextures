# M5.2 — Peer Review (Mobile)

> Complete assigned peer reviews on a phone: read a classmate's submission, score it
> against the rubric, leave comments, and submit — plus see reviews received.
> Source: 3.15 peer review & peer assessment, `peer_review_http.go`, `peer-reviews-page`.

## Metadata

| Field | Value |
|---|---|
| **Epic** | M5 — Assignments & Submissions |
| **Severity** | MAJOR — peer review is frequently required, time-boxed work students need to do anywhere |
| **Status (today)** | **DONE** — assigned list, submission preview + rubric scorer, submit/edit, received feedback, offline cache + queue on iOS and Android |
| **Estimated effort** | M |
| **Platforms** | iOS (SwiftUI), Android (Compose) |
| **Backend** | Minimal read endpoint — `GET /api/v1/peer-review/allocations/{id}` (submission + rubric for reviewers); peer-reviewer attachment access |
| **Depends on** | [M5.1](M5.1-assignment-submission.md) (submission preview), [M6.1](M6.1-grades-feedback.md) (rubric UI), [M3.2](M3.2-course-files.md) (attachment preview) |

---

## 1. Problem Statement

Peer review tasks are assigned with deadlines and count for credit, but mobile couldn't
do them — so a phone-first student missed required work. They needed to read assigned
peers' submissions and submit rubric-based feedback on device.

## 2. Goals

- See **assigned** peer reviews (due dates, progress) and complete each: read the
  submission, score the rubric, comment, submit.
- See **reviews received** on one's own work.
- Support anonymity rules the server enforces.

## 3. Non-Goals

- Configuring peer-review settings (instructor/web). Calibrated/peer-grade weighting
  config stays server-side.

## 4. User Stories

- **As a student**, I open my 3 assigned reviews, read each peer's essay, score the
  rubric, and submit feedback.
- **As a student**, I read the anonymous feedback peers gave me.

## 5. What already exists (reuse)

- **Server**: `peer_review_http.go` (assigned list, submit, received) plus allocation
  detail GET for reviewer submission + rubric.
- **Mobile**: M6.1 rubric patterns, M3.2 `FilePreviewView`, M5.1 submission rendering,
  M0.2 offline cache/outbox.

## 6. Functional Requirements

- **FR-1.** A **Peer Reviews** surface lists assigned reviews with peer-submission
  reference (respecting anonymity), due date, and done/remaining state.
- **FR-2.** Opening one renders the **submission to review** (text + attachment preview)
  and a **rubric** scorer + comments.
- **FR-3.** Submitting persists scores/comments via the peer-review endpoint and marks
  the review complete; resubmission/edit allowed where policy permits.
- **FR-4.** A **received** view shows feedback on the student's own work per release
  policy/anonymity.
- **FR-5.** Anonymity the server applies is honored (never reveal redacted identity).

## 7. Non-Functional

- **Offline**: read assigned items from cache; submitted reviews queue (M0.2).
- **Accessibility**: rubric + comment inputs labelled; preview accessible.

## 8. Design / Approach

`Features/PeerReview/` (iOS) / `features/peerreview/` (Android): list, detail (submission
+ `RubricScorerView` + comments), received view. More hub entry when `ffPeerReview` is on;
assignment detail links to received feedback. API in `LMSAPIPeerReview.swift` / `LmsApi.kt`.

## 9. Files touched

**iOS** — `Features/PeerReview/*`, `Core/LMS/PeerReviewLogic.swift`,
`LMSFeatureModelsPeerReview.swift`, `LMSAPIPeerReview.swift`, `RubricScorerView.swift`,
`MobileDestinations.swift`, `AssignmentDetailView.swift`, `ProfileViewSections.swift`,
`LexturesTests/PeerReviewLogicTests.swift`, `project.pbxproj`.

**Android** — `features/peerreview/PeerReviewScreens.kt`, `core/lms/PeerReviewLogic.kt`,
`LmsFeatureModelsPeerReview.kt`, `LmsApi.kt`, `MobileDestinations.kt`,
`AssignmentDetailScreen.kt`, `ProfileTab.kt`, unit tests.

**Backend** — `peer_review_http.go` (GET allocation detail), `peerreview/repo.go`,
`assignment_submission_attachments_http.go` (peer reviewer access).

## 10. Acceptance Criteria

1. Assigned reviews list with due dates and progress, honoring anonymity. ✅
2. A review shows the peer submission (text + attachments) and a working rubric scorer + comments. ✅
3. Submitting records scores/comments identical to web and marks the review done. ✅
4. Received feedback displays per release policy. Both apps build. ✅

## 11. Phasing

1. Assigned list + detail (read submission) + rubric scorer. ✅
2. Submit + complete + edit. ✅
3. Received view; offline queue (M0.2). ✅
4. Android parity; verification. ✅
